export type LocalNote = {
  id: number;
  glpi_user_id: number;
  note: string;
  created_at: string;
};

export type Note = {
  id: number;
  glpi_user_id: number;
  note: string;
  created_at: string;
};